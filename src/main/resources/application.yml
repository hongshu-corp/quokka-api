logging:
  pattern:
    dateformat: "yyyy-MM-dd'T'HH:mm:ss.SSS'Z', UTC"

spring:
  jpa:
    hibernate:
      ddl-auto: none
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    show-sql: true

  liquibase:
    change-log: classpath:/db/changelog-master.yaml

